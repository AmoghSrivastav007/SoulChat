"use client";

// Sentry stub — install @sentry/nextjs and set NEXT_PUBLIC_SENTRY_DSN to enable.
// This file is intentionally a no-op until the package is added.
export async function initSentry(): Promise<void> {
  // no-op
}
